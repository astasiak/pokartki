<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.astasiak.pokartki" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Base.Theme.AppCompat">
        <activity
            android:name=".gui.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".gui.DirectionsActivity"/>
        <activity android:name=".gui.SetsActivity"/>
        <activity android:name=".gui.SetActivity"/>
        <activity android:name=".gui.CardActivity"/>
        <activity android:name=".gui.OptionsActivity"/>
    </application>

</manifest>
